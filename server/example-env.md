# Local MongoDB:
# MONGODB_URI=mongodb://localhost:27017/vivid-nexus

# MongoDB Atlas (free tier works fine):
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/vivid-nexus?retryWrites=true&w=majority

PORT=4000
